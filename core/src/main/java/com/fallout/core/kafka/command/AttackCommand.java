package com.fallout.core.kafka.command;

import com.fallout.core.enums.ActionType;

public class AttackCommand extends Command {

    public AttackCommand(ActionType actionType) {
        super(actionType);
    }
}
